# 2026-03-16

## Activities
- Godot Farm game development session with Jerry
- Fixed particle effects system (NaturalParticles with dust effect)
- Removed global shader background distortion
- Added localized animated lights instead of global effects
- Fixed camera bounds calculation for anchor_mode = 0
- Fixed UI button placement and interactions
- Added animal sound effects (cow, sheep, zebra, pig)
- Added ambient audio (wind, leaves, birds)
- Generated UI icons using Gemini NanoBanana (coin, exp, level)
- Created sickle harvest tool and planting menu
- Fixed HUD layout with image buttons

## Issues Fixed
- Camera limits now correctly calculated for top-left anchor mode
- Particle effects now more natural with proper turbulence settings
- Background shader removed (was causing distortion)
- Animal names added to all animal instances for sound playback
- UI buttons now use extracted spritesheet images

## Afternoon Session - Major Recovery

### Critical Incident
- **Mistake**: Executed `rm -rf .godot` and `git checkout -- .` without checking status
- **Result**: Lost all new files created today (sickle tool, watering tool, shop, etc.)
- **Recovery**: Had to rebuild all features from scratch

### Rebuilt Features
1. **HUD with Icon Buttons** - Using `button_sickle.png`, `button_water.png`, `button_backpack.png`, `button_shop.png`
2. **Sickle Harvest Tool** - Drag to harvest mature crops with `res://assets/ui/button_sickle.png`
3. **Watering Can Tool** - Click to water crops with `res://assets/ui/button_water.png`
4. **Planting Menu** - Popup menu from backpack button, replaced seed toolbar
5. **Economy System** - Planting costs gold, harvesting earns gold
6. **Crop Removal** - Visuals properly removed after harvest

### Camera System Changes
- Added zoom limits (0.35 - 3.0) to see entire map
- Fixed drag controls with middle-mouse fallback
- Initial zoom set to 0.35 (maximum view)

### Plot Alignment Debug Mode
- Added real-time plot adjustment system
- Keys: 1-9,0 to select plots
- Arrow keys: Move position
- W/S: Adjust width, A/D: Adjust height
- +/-: Change adjustment speed
- R: Rebuild all plots

### Lessons Learned
- **ALWAYS** `git push` before any destructive operations
- **NEVER** use `rm -rf` or `git checkout -- .` without backup
- Created safety rule: Push to GitHub before any file deletions

## Todo
- Complete remaining farm game features (user database)
- Test full game flow
- Fine-tune plot alignment with debug mode
